#pragma once
#include <vector>
#include <string>

namespace experis
{
//Todo add using or template
void GetDelimiterIdx(std::vector<unsigned short>& a_delimiterIdx, const char a_delimiterType, const std::string& a_instruction);

} // namespace experis

