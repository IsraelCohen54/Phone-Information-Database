#pragma once

#include <string>

namespace experis
{
//using BigRangeWithMinus = long long;

bool OnlyDigits(const std::string& untrust_num);
bool OneToFive(const std::string& untrust_num);

} // namespace experis
